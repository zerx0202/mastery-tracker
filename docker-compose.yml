services:
  backend:
    build: .
    container_name: riot-backend
    env_file: .env
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/code/data
      - ./tools:/code/tools:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
