services:
  backend:
    build: .
    container_name: riot-backend
    env_file: .env
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/code/data
    networks:
      - proxy

networks:
  proxy:
    external: true
